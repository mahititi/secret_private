
  <h1 class="members">Tous les membres</h1>

  <table class="table table-striped">
  <thead class="thead-light">
    <tr>
      <th scope="col">Prénom</th>
      <th scope="col">Nom</th>
      <th scope="col">Email</th>
      <th scope="col">Profil</th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
  <tr>
    <td>
      <%= user.first_name %>
    </td>
    <td>
      <%= user.last_name %>
    </td>
    <td>
      <%= user.email %>
    </td>
    <td>
      <%= link_to 'Voir le profil', user_path(user.id) %>
    </td>
  </tr>
  <% end %>
  </tbody>
  </table>
